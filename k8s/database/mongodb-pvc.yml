kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: mongo-pvc
  namespace: chat-app
spec:
  resources:
    requests:
      storage: 1Gi
  storageClassName: mongodb-storage
  accessModes:
    - ReadWriteOnce